% ============================================================
%  part2/ch09-entropy.tex  —  Chapter 9: Entropy
% ============================================================

\chapter{Entropy and Information}
\label{ch:entropy}

\epigraph{%
  Entropy is not a measure of disorder.\\
  It is a measure of our ignorance.%
}{Edwin T.\ Jaynes}

\section{Entropy as missing information}

\begin{definition}[Shannon entropy]
  For a discrete probability distribution $\{p_i\}$,
  \[
    H = -\sum_i p_i \log p_i.
  \]
\end{definition}

\begin{proposition}
  $H \geq 0$, with $H = 0$ iff the distribution is a point mass.
\end{proposition}

\begin{TechNote}[Entropy and reconstruction]
  In the reconstruction framework of Part~I, entropy measures
  the size of equivalence classes.
  If $\pi^{-1}(m)$ has measure $\mu(\pi^{-1}(m))$, the
  representational entropy is
  $S_\pi(m) = \log \mu(\pi^{-1}(m))$.
  High entropy means large equivalence class means high
  reconstruction ambiguity.
  This connection will be formalised in Chapter~\ref{ch:clio}.
\end{TechNote}

\section{Entropy production}

\begin{definition}[Entropy production rate]
  For a field $S(x,t)$ satisfying
  $\partial_t S + \nabla \cdot \mathbf{J}_S = \sigma_S$,
  the local \emph{entropy production rate} is $\sigma_S \geq 0$.
\end{definition}

\begin{proposition}[Second law]
  For an isolated system with $\mathbf{J}_S \cdot \hat{n} = 0$
  on $\partial\Omega$,
  \[
    \frac{d}{dt}\int_\Omega S\, dx
    = \int_\Omega \sigma_S\, dx \geq 0.
  \]
\end{proposition}

\begin{AdmPreview}
  In RSVP, the entropy field $S(x,t)$ plays a dual role.
  Globally it increases (second law).
  Locally it can decrease in coherence-building regions
  (lamphron-dominated zones) while increasing faster in
  void regions (lamphrodyne-dominated zones).
  The two rates compensate to maintain global increase.
  This is the thermodynamic version of the lamphron/lamphrodyne
  duality introduced in Chapter~\ref{ch:lamphron-lamphrodyne}.
\end{AdmPreview}

\WhatSurvived{Entropy projection $S \mapsto \int S\,dx$}{%
  Total entropy\\
  Global thermodynamic state%
}{%
  Local entropy distribution\\
  Spatial gradients\\
  Entropy production field%
}{%
  Partial; total entropy alone\\
  underdetermines local state.%
}{%
  $\ker$: all redistributions\\
  preserving total entropy.%
}
