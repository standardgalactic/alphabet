defaults:
  - _self_
  - modules/rsvp
  - modules/ebssc
  - modules/spherepop
  - modules/tartan
  - modules/clio
  - presets: minimal

backend: "torch"

logging:
  log_every: 50
  plot_results: false

simulation:
  steps: 200
  grid_size: 64
